cmake_minimum_required(VERSION 3.5)
include(CheckCCompilerFlag)
project(zstd-jni C ASM)
file(STRINGS "zstd-jni/version" NATIVE_VERSION)
find_package(JNI)
include_directories("zstd-jni/src/main/native" "zstd-jni/src/main/native/common" "zstd-jni/src/main/native/compress" "zstd-jni/src/main/native/decompress" "zstd-jni/src/main/native/dictBuilder" "zstd-jni/src/main/native/legacy" "dhjni")
if (JNI_FOUND)
    include_directories(${JNI_INCLUDE_DIRS})
endif (JNI_FOUND)
file(GLOB_RECURSE SOURCES RELATIVE ${CMAKE_SOURCE_DIR} "zstd-jni/src/*.[cS]" "dhjni/*.[cS]")
add_library(zstd-jni-${NATIVE_VERSION}-dhcompat SHARED ${SOURCES})
add_definitions("-DZSTD_MULTITHREAD=1")
add_definitions("-DZSTD_LEGACY_SUPPORT=4")
CHECK_C_COMPILER_FLAG(-Oz COMPILER_SUPPORTS_OZ)
if(COMPILER_SUPPORTS_OZ)
    add_definitions("-Oz")
endif()
set (CMAKE_SHARED_LINKER_FLAGS "-Wl,-z,max-page-size=16384")
